# {{表名}}（{{表中文名}}）

> 状态：✅ 已实现 / 🚧 规划中 | 关联模块：{{模块}}
> 事实源：{{schema SQL 路径}} | 关联文档：{{设计文档}}、{{接口文档}}

**用途**：{{一句话说明该表的业务用途}}。

## 字段

| 字段 | 类型 | 空 | 默认 | 含义 |
|------|------|:--:|------|------|
| id | {{类型}} | 否 | AUTO_INCREMENT | 主键 |
| {{字段}} | {{类型}} | 是/否 | {{默认}} | {{业务含义}} |
| created_at / updated_at | DATETIME(3) | 否 | 当前 | 通用时戳（{{是否含 updated_at}}） |

## 索引

| 名称 | 类型 | 字段 | 用途 |
|------|------|------|------|
| pk_{{表}} | 主键 | id | — |
| {{索引名}} | 唯一/普通/全文 | {{字段}} | {{用途}} |

## 枚举

`{{枚举字段}}`：

| 值 | 常量 | 含义 | 触发条件 |
|----|------|------|----------|
| {{0}} | {{CONSTANT}} | {{含义}} | {{条件}} |

## 业务规则

- {{规则 1：状态流转 / 约束 / 计算逻辑}}
- {{规则 2：与其他表的关系}}

> 字段级完整说明见 [data-dictionary.md](../data-dictionary.md)（若存在）。
